# Google API Key for Gemini Image Generation
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# Model Configuration - Use different models for different tasks
# Seed Image Model: Used for generating character reference images from uploaded photos
SEED_IMAGE_MODEL=gemini-2.0-flash-exp

# Story Text Model: Used for text personalization and story processing
STORY_TEXT_MODEL=gemini-2.0-flash-exp

# Story Image Model: Used for generating story illustrations
STORY_IMAGE_MODEL=gemini-2.5-flash-image-preview

# Default Model: Fallback model for backward compatibility
DEFAULT_MODEL=gemini-2.5-flash-image-preview

# Legacy Model Configuration (deprecated - use specific model fields above)
# MODEL=gemini-2.5-flash-image-preview

# Optional: Generation Settings  
# MAX_PAGES=15

# Optional: Rate Limiting Settings (for handling quota errors)
# RETRY_ON_QUOTA_ERROR=true
# DELAY_BETWEEN_IMAGES=5
# MAX_RETRIES=3  
# BASE_RETRY_DELAY=60